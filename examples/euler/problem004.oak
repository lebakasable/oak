fn main() {
   let res = 0
   for let i = 100, i < 1000, i += 1 {
      for let j = 100, j < 1000, j += 1 {
         let a = i*j
         let b = 0
         for a > 0 {
            b = b*10 + a%10
            a /= 10
         }
         if b == i*j {
            if b > res {
               res = b
            }
         }
      }
   }
   print res
}
